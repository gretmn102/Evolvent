General:
    ☐ Воплотить стратегию постройки всего проекта: сначала строится Parse, затем запускается его исполняемый файл. Он создает файл, который используется для постройки проекта Tolerance. Полученный исполняемый файл нужно собрать с помощью ILMerge такой командой: `$(SolutionDir)\ILMerge\merge_all.bat" "$(SolutionDir)" "$(TargetPath)" $(ConfigurationName)`.